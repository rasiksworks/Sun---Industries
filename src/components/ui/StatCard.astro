---
interface Props {
  number: string;
  label: string;
}

const { number, label } = Astro.props as Props;
---

<div class="rounded-lg border border-haze bg-haze/30 px-6 py-10 text-center">
  <p class="stat-number text-ink">{number}</p>
  <p class="mt-3 text-sm text-muted">{label}</p>
</div>
